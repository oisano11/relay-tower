# 更新日志

技术细节与验收记录见 [docs/repair-plan.md](docs/repair-plan.md)。

## 2026-09-25

### 修复：上游改价后，塔台的进价不跟着变，也不提醒

- 改价提醒恢复了。9 月 20 日起，塔台一发现进货倍率变了，处理程序就会中途报错，所以控制台不弹窗、Telegram 不推送，倍率历史也没有记录。
- 进价重新自动跟着上游走。9 月 21 日起，账号在 Sub2API 里没开「自动同步倍率」时，塔台一直用账号里填的倍率，上游涨价、降价它都不知道。现在只要 Sub2API 最近一小时内查到过上游价格，塔台就按上游价格算进价。
- 上游查不到价格、查询失败或数据过期时，塔台用账号里填的倍率，过期的旧价格不会盖过手填的值。
- 在塔台手动改进价时，如果这个账号能查到上游价格，大约半小时内会被上游最新价格替换。想固定用手填的价格，要在 Sub2API 里关掉这个账号的上游计费探测。
- Sub2API 后台账号里的倍率不受影响。想让它也跟着上游变，要在 Sub2API 给账号打开「自动同步倍率」。

### 修复：删除分组又慢又经常删不掉

- 点删除后先检查，按钮显示「检查中…」。分组里还绑着客户 Key、客户订阅，或者被别的分组设成了备用分组，就不让删，弹窗说明要先在 Sub2API 后台做什么。因为分组一删，Sub2API 会直接拒绝这些 Key。
- 能删的分组，确认框里会列出要一起停用的账号，也就是还在接单、而且只在这个分组里的账号。确认一次就删完。以前遇到这种账号一律拦下，几乎所有分组都删不掉。
- 删除只等数据库写入这一步，清缓存、重读账号放到后台去做，控制台不再卡住。
- 售价是 0 的分组也能删了。
- 删分组时顺带清掉这个分组的用户可用名单和组合路由，和 Sub2API 自己删分组时一样。
- 部署脚本：在中文环境的终端里运行，会报「unbound variable」停下。现在可以正常部署了。

### 新功能：自动发现上游新建的 Key，一键接入本站

- 在上游（Sub2API 搭建的站）新建的 API Key，会自动出现在控制台【⚙️ 系统管理 → 🆕 接入上游新 Key】，菜单上显示待接入的数量。塔台每 10 分钟检查一次，发现新 Key 时在报错信息盒和 Telegram 各提醒一次。
- 选好放进本站哪个分组、改好名称，点「接入」，塔台就在本站 Sub2API 里建好账号：照着同一家上游已有的账号复制设置，只换 Key、名称、进价和分组。分组里已有账号时，新账号先当备用；分组还是空的，就直接当主调。
- 自动拦住：平台对不上的分组（例如 Claude 的 Key 只能进 Claude 分组）、进价不低于售价的分组、这家上游还没有同类型账号可以参照、同一个 Key 重复接入。数据库里也有同样的检查，任何一步失败都不会写入。
- 不想接的 Key 点「删除」，以后不再提示；可以恢复，不影响上游。
- 完整的 Key 只在服务器上使用，页面只显示末 4 位。
- 暂不支持 New-API 搭建的上游。只保存了令牌、没有保存账号密码的上游，令牌过期后要在「上游供应商后台」重新填写账号密码。

## 2026-09-24

### 控制台按钮梳理

- 顶栏从 9 个按钮减到 4 个：刷新数据、报错信息盒、充值看板、⚙️系统管理。以前在 1440 宽的屏幕上，右边 6 个按钮会跑到屏幕外，现在 1280 宽也能完整显示。其余功能收进「系统管理」菜单，分成「自动切号 / 分组与账号 / 上游供应商 / 系统」四类。
- 合并重复功能：「立即检测」和「同步后台」做的是同一件事，合成「刷新数据」；「同步上游余额」只保留余额卡片上的一个。
- 去掉容易误操作的入口：
  - 顶栏的全站「当前主调」下拉框：选一下就立刻改线上路由，没有确认。现在主调、副调只在分组里设置。
  - 「模拟改价」「模拟变价演练」：开发测试用，点了会写入一条假的涨价记录并推送告警。
  - 分组横幅上的「纳入全组合规渠道」：会把别的分组的账号拉进来，变成自动切号用不了的共享账号。
- 修复：
  - 「同步上游余额」点一次会同步两遍。
  - 首页默认视图里的「改售价」打不开，只提示「尚未关联分组」。
  - 分组编排窗口会带出上一个分组的主调，副调和备选总显示为空，保存时可能设错账号。
  - 涨价提醒里的「一键切换」会去别的分组挑账号，有时什么都没切却提示「已切换」。现在只在同一个分组里换更便宜的账号，并提供「去这个分组看看」。
- 「全部纳入自动调度 / 全部人工停用」执行前需要确认，没有筛选时会额外提醒。
- 找回「业务分组管理」（新建、删除分组）的入口。
- 首页「通道调度与风控」卡片显示真实的自动切号状态和切回间隔。

### 自动切号

- 共享账号一键拆分：把挂在多个分组下的账号拆成每组一个独立账号，之后每组都能单独自动切换。
- 切号预演（只看不改）：逐组显示现在会怎么切、每个账号能不能当备用，以及原因。
- 请求少的分组也能确认故障：连续失败 3 次后，塔台会主动发一次真实请求验证，验证也失败才换号。
- 分组的自动切号设置默认跟随全站，只保存和全站不同的项。

### 部署

- 新增 `deploy/deploy.sh` 一键部署：自动识别服务器上的塔台目录和配置，部署前先备份，健康检查失败自动回滚。
