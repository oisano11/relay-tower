# 修复计划与验收

范围：修复项目分析中的 14 类确定性问题；不连接生产数据库、不执行真实切线或发送 Telegram 消息。

1. 部署与认证：镜像包含代码；回环判断使用 socket；Cookie 容错；Telegram 仅授权个人；请求统一兜底。
2. 网关：独立代理模块；停用通道不得选中；保留查询参数；替换上游凭证；首字超时、断流取消与真实指标。
3. 数据一致性：数据库错误必须上抛；SQL 经 stdin 传递；切线与改价先提交远端事务再更新内存；充值检查目标存在。
4. 巡检与审批：修复故障分组引用；统一模型 URL；识别冷备；审批仅修改来源通道并同步数据库。
5. 调度：人工锁定独立于优先级；按当前组售价判断利润；使用真实连续失败与首字阈值；共享账号不执行跨组停调。
6. 验收：隔离回归测试、原巡检测试、语法检查、CI 启动检查；更新文档中的能力边界。

各批次以测试为准。跨组调度支持一个渠道属于多个分组：专属独立渠道在单主独占模式下进入冷备 (`schedulable=false, priority=10`)；跨组共享渠道保留全局调度开关 (`schedulable=true`) 仅调低当前组内优先级 (`priority=10`)，杜绝跨组误杀，不修改上游数据库结构。异步数据库迁移和前端整体拆分列为后续架构工作，避免与本次正确性修复混杂。

## 进度

- [x] 部署代码与认证修复
- [x] 网关
- [x] 数据一致性
- [x] 巡检与审批
- [x] 调度
- [x] 本地回归验证与说明
- [ ] Docker 镜像实际构建与容器启动（本机无 Docker；已加入 CI，尚未运行）

验收入口：`cd upstream-monitor && npm test` 汇总巡检、认证、Telegram、网关、数据库失败、角色计划及服务启动回归测试。CI 检查根目录 JavaScript 模块及前端脚本语法，并构建镜像后启动无生产卷、无外部网络的临时容器，检查 `/api/auth/status`。本机无 Docker 命令，镜像真实运行尚未本地验证；CI 待运行。CI 配置不等于部署验收完成，实际执行结果由本轮最终验收记录确认。

文档同步范围：缓存亲和的实际边界、倍率默认 5 分钟与探活默认 3 小时周期、Telegram 个人授权流程、共享账号排他切换限制。数据库同步仍使用同步子进程，慢查询仍可能阻塞事件循环；异步数据库访问与后台任务拆分尚属后续工作。

## 本轮验收记录（2026-09-13）

- `npm test`：巡检测试 12/12；Node 测试运行器 51/51（包含角色规划脚本，其内部另检查 6 个场景）。
- 所有应用模块、前端 app.js 和测试脚本通过 `node --check`；`git diff --check` 通过。
- 服务启动测试使用真实本地 HTTP 和 VM 内存文件系统：数据库不可用仍可登录；错误 Cookie 不崩溃；写数据库失败返回错误；无可用渠道返回 503。
- 网关集成测试验证 URL/凭证/请求体、SSE 分片、TTFT、取消、流内错误和压缩响应；压缩 SSE 上游若忽略 identity 请求，则明确返回 502，不把它误计成首字超时。
- 调度测试验证严格人工锁定不改价、自动主调可回切、数据库失败状态不变、售价与路由统一提交、共享账号保护和当前组利润判断。
- 未连接真实 Sub2API/PostgreSQL/Redis 或 Telegram，未修改生产数据，未部署。真实数据库版本兼容与镜像运行仍需要部署环境验证。

## 自动切号与上游分组：实际页面核对（2026-09-13）

用户当前重点调整为收拢调度入口与发现上游新增分组，不是新增模型。已只读查看本站 Sub2API 添加账号表单及 ZT 上游创建密钥表单，未提交任何账号、密钥或配置变更。

- 本站添加账号：Base URL、API Key 与本站分组分别填写；本站分组允许多选。
- 上游创建密钥：选择上游分组。ZT 页面实际可见福利 GPT（动态倍率）、纯血 Pro、1000PLUS 池、001GROK 等，并展示倍率和部分说明；已有密钥也显示绑定的组。
- 应发现的是“供应商 + 上游分组”，即使域名相同也必须区分。现有抓取只有模型输出，不能作为新增渠道发现。
- **已完成的兼容修复：监控与 SUB2API 统一采用数字越小越优先（主调 1 / 副调 10 / 备选 20 / 备用 100）；角色与人工停用标记分离，未执行启动时批量改写。**

上游分组发现仍按以下顺序实施：

1. 建立上游分组目录与本站账号来源映射；验证供应商实际接口、分页、可见权限和报价字段后接入，不猜测接口路径。
2. 抓取新增组及价格变化，展示来源、价格基准、可用范围和观测时间；抓取失败不得标为全体下架。
3. 新分组先进入待接入列表。用户提供适用于该上游组的 Key 后接入本站账号，选择本站业务组，验证后加入候选；不因发现便宜组而新建本站销售组或改价。
4. 页面收拢为“上游资源”和“业务调度”。全局开关仅作总开关，每组只保留一份有效切换规则；其他入口只跳转到同一管理面板。

### 上游分组目录已实现（2026-09-13）

- 扫描器对每个已配置上游面板以只读方式尝试兼容的分组接口，使用“面板 ID + 上游分组 ID”作为稳定键。
- 目录保存于 `upstream-monitor/data/upstream_group_catalog.json`，记录名称、成本/销售倍率（若上游返回）、模型/能力、API 地址、首次发现和最后抓取时间。
- 新增分组与价格/属性变化会进入扫描报告；接口不可用时保留旧记录并标记 `stale`，不会批量下线。
- 新增 `GET /api/upstream/groups` 和 `POST /api/upstream/groups/scan`，均不创建本站账号、业务分组或修改售价。真实供应商接口字段仍需部署环境只读验证。

以上是本次实际核对后的上游分组阶段计划；真实供应商分组发现仍需在部署环境用只读接口验证。

## 自动切号第二阶段（当前实施）

目标：已接入的同组账号自动容灾、充值后自动恢复；人主要负责充值。上游新增分组发现另行实施。

1. 将故障判断抽为独立策略：连续探活失败或足够的实际请求失败确认故障；新鲜可信欠费记录立即隔离。查余额失败与陈旧观测不能伪装成新欠费。
2. 统一执行切换：同组、有资格、健康、满足明确模型配置且成本不超过售价的候选按成本选择。故障切换不受回切冷却阻挡，不再要求逐次人工审批，也不修改售价。
3. 恢复防抖：恢复后需连续健康观测和稳定等待；回切便宜号另需满足价差与冷却。已确认欠费需重新查到正余额，不能仅凭模型列表能打开判为充值成功。
4. 单独保存人工停用标记，冷备用不等于人工停用；遵守全局及分组开关。探活只记录状态，不独立永久关闭分组。
5. SUB2 原生优先级统一为数字越小越优先；移除启动时强制定性的隐式写入。部署前不修改线上账号。
6. 验收覆盖瞬断、欠费、恢复、防反复切换、全部不可用、人工停用、分组/模型范围和数据库失败。

前端入口已收拢：自动切号只保留统一策略设置和每组策略覆盖；故障切换不再弹人工批准窗口，也不再提供重复的按成本定性按钮。人工停用仍是明确的候选池排除操作；上游巡检中的新模型/同价渠道待办仍保留人工确认，因为它们可能影响对外可用范围。

自动化边界：需要预先接入可用账号、正确报价以及可查询的上游余额。若整组欠费或没有合格备选，系统提示处理，不凭空创建凭证或提高售价。当前共享账号受 SUB2 全局开关限制，不跨组强制切换。

## 后续架构优化

1. 将同步数据库访问迁为异步连接池，或把巡检与网关放到不同进程，消除慢查询阻塞流量的可能。
2. 在 Sub2API 支持的接口与数据结构基础上实现真正的分组级角色；当前共享账号采用明确拒绝/跳过，不擅自修改上游表结构。
3. 拆分财务、管理接口与前端页面模块。网关与路由规则已先独立成模块。
4. 完善生产观测：网关连续故障使用五分钟实际请求窗口；Sub2API 历史统计只用于失败率/延迟参考，不把累计错误冒充连续失败。

## 自动调配逻辑修复（2026-09-24）

目标：让塔台对 Sub2API 已接入渠道的“故障切走 → 充值/恢复 → 自动切回”闭环在真实环境可用。

1. **欠费标记不再永久卡死**：欠费证据按“最新可信观测”判定。查不到余额（unknown）的账号，由一次真实 1 token 生成成功解除欠费；不限额账号在额度报错窗口过去且余额刷新后解除。修正 baseUrl 以 `/v1` 结尾时余额请求变成 `/v1/v1/usage` 的问题。
2. **OAuth / Setup-Token 账号不再被误判离线**：无 API Key 的账号标记 `passiveHealth`，不做 HTTP 探活，只按真实请求统计判断；Anthropic 平台探活补 `anthropic-version` 头。
3. **切回前必须真实生成成功**：请求级故障（失败率、首字超时、连续失败）后，仅 `/v1/models` 可达不再算恢复；调度器对需要证明的账号每 5 分钟（`generationProbeIntervalMs`）发一次 1 token 请求，流内首帧错误也判失败。`requireGenerationProbe: false` 可关闭。
4. **无备选时不再整组停服**：只关停确认欠费或人工停用的独占账号；失败率高、首字慢、探活失败、倍率高于售价的主调保持服务并告警。
5. **关键字例外渠道不被自动化触碰**：当主调是例外渠道时整组保持人工控制；切线与耗尽处理都不会关停例外渠道。人工停用（`autoSwitchDisabled`）仍可被迁出。
6. **主调判定口径统一为 `accounts.priority`**：已核对 Sub2API 源码，调度器最终按账号全局优先级排序，`account_groups.priority` 只影响查询顺序，不能实现分组级主备。
7. **过期决策闸门生效**：路由版本在评估前取值。
8. **额度识别收紧**：Sub2API 统计侧不再用宽泛的 `quota|balance`，429 限流不再算欠费；新增 Anthropic “credit balance is too low”。
9. **候选排序可选**：`candidateOrder: "cost"`（默认，成本优先）或 `"role"`（按主调/副调/备选/备用优先级）。
10. **Redis 刷新改为 SCAN**：不再用阻塞的 `KEYS`。

验收：`npm test` 136/136；新增 16 条回归测试在旧代码上失败、修复后通过。未连接真实 Sub2API/PostgreSQL/Redis，未修改线上数据。

尚未处理（需架构调整）：
- 共享账号的分组级切换：Sub2API 没有分组级调度开关，已由下面的“共享账号拆分”解决。
- 同步数据库调用仍在主进程执行，SSH 模式下可能阻塞 `/v1` 网关；`/v1` 网关使用全局单一主调、绕过 Sub2API 计费，多分组场景建议客户端直连 Sub2API。

## 共享账号一键拆分（2026-09-24）

控制台【⚙️ 系统管理 → 🧩 拆分共享账号】：先读取 Sub2API 最新数据并预览，勾选确认后执行。接口：`GET /api/accounts/split-plan`（只读预览）、`POST /api/accounts/split`（`{ accountIds: [...] }`）。逻辑在 `account-split.js`。

- 原账号保留在主分组；其余每个分组各新建一个账号（名称为“原名 · 分组名”，备注标明来源），并把原账号移出这些分组。
- 新账号整行复制原账号：Key、模型映射、倍率、代理、并发上限（照抄原值）、优先级、调度开关，拆分瞬间各组路由不变。上游钱包仍是同一个，注意总并发会变成原值 × 分组数。
- OAuth / Setup-Token 等无 API Key 账号不拆（刷新令牌会轮换，复制后会互相失效），未启用的账号不拆。
- 所有选中账号在一个事务内执行；执行前逐个校验账号存在且分组与预览一致，否则整体回滚。
- 已用 Sub2API 官方迁移脚本建出的 PostgreSQL 16 真实表结构验证：复制、分组迁移、并发与优先级保留、序列号连续、分组变动时中止均符合预期；IDENTITY 主键同样可用。

## 轮换补强与上线验证（2026-09-24）

- **不提供 `/v1/models` 的上游可以当备选了**：探活返回 404/405 时改用真实生成探测（默认每 5 分钟 1 token）。最近一次生成成功才算在线；失败或过期记为“未知”（不当备选，但也不累计探活失败，真实流量故障仍由请求统计判断）。上游巡检不会覆盖这类账号的状态。
- **探测模型自动回退**：候选顺序为上次成功的模型 → 模型映射 → 分组所需模型 → 上游发现的模型 → 平台默认模型，最多试 4 个。只有“模型不存在 / 该分组无此模型”才换下一个；欠费、服务器错误不会浪费额外请求。全部不存在时状态为 `unknown_model`，提示去 Sub2API 配置模型映射。
- **切号预演（只读）**：控制台【⚙️ 系统管理 → 🧭 切号预演】/ `GET /api/auto-switch/preview`，与真实切号共用同一套输入和决策逻辑，逐组列出当前主调、将切到谁、每个账号为什么能或不能当备选。不写数据库、不改运行时状态。

### 上线验证步骤

1. 部署后打开【🧭 切号预演】，逐组核对：当前主调是否正确；每组至少有一个“可做备选”；“不可用”账号的原因是否符合实际。共享账号先用【🧩 拆分共享账号】拆开。
2. 挑一个非核心分组，确认它有可用备选，然后让主调故障：在 Sub2API 把主调的 API Key 改成错误值，或用掉它的余额。
3. 等 1–3 分钟，预演应显示“将切换”，随后自动切到备选，告警里出现切号记录，该组客户端请求恢复正常。
4. 恢复主调（改回 Key 或充值）。约 5–10 分钟内：生成探测成功 → 连续 3 次健康观察 → 冷却结束后自动切回原主调（或更便宜的账号）。
5. 若没有切回，查看预演里该账号的说明：“等待真实生成成功”说明探测还没成功，可看生成探测的报错；`unknown_model` 说明要配置模型映射。

## 与「中转站」目录合并（2026-09-24）

线上使用的「中转站」目录与本仓库同一提交（55e8c79），但各自有未提交改动。已按三方合并把「中转站」的全部改动并入本仓库（逐行核对无遗漏）：生产阈值（连续失败 30、探活失败 20、欠费 20、最小样本 50、失败率 70%）、非独占多主调、默认关闭低价自动切回、切号清理会话粘性缓存、余额查询兼容 `/v1` 与 billing 接口、面板并发同步、单通道分组保护、余额同步按钮等。

合并后修正两处与这些设置叠加产生的问题：
- `autoRecoverLowestCost=false` 只关闭“为省钱换线”，原主调充值/恢复后仍会自动切回。
- 非独占模式下切号会把故障来源降为备用优先级（欠费/人工停用则停调），否则它仍是优先级 1，Sub2API 会继续分流给它。

部署：`./deploy/deploy.sh <服务器别名>`（沿用 ~/.ssh/config 的端口、密钥、代理；自动识别免密 sudo 与塔台目录）。测试 152/152。

## 部署脚本、切号面板与低流量故障确认（2026-09-24 下午）

- **部署脚本识别服务器实际结构**：线上塔台可能是独立 release 目录（有自己的 compose 项目、端口和命名卷）。`deploy.sh` 现在从运行中容器读取项目名、配置文件、端口和数据卷；目录里没有 `.env` 时 `DOCKER_GID` 取自 docker.sock。修复 macOS bash 3.2 空数组报错、健康检查端口写死 3300、重建失败不回滚。
- **切号预演**：「重新推演」不再关窗重开，保留滚动位置，并提示与上次相比有没有变化（变化的分组标黄；余额数字变化不算）。与当前账号优先级相同的账号标为「同时在用」（Sub2API 会一起分流量）；「不参与」写明原因（例如分组名含「通用」）。
- **自动切号面板**：顶部显示各分组的保护情况（有备用 / 没有可用备用及原因 / 人工管理 / 当前出故障且无备用）；说明改为与实际逻辑一致的大白话（原文“单主独占”“低价回切”与线上配置不符）。保存时只提交面板上的设置，不再把首字超时、欠费确认次数等隐藏项覆盖成写死的值。
- **请求少的分组也能切走**：原规则只看最近 5 分钟，请求少时“连续失败 30 次 / 样本 50 次”永远凑不够，上游“能连上但生成失败”时会一直停在坏账号上。现在窗口内请求数不足连续失败门槛、且最近 3 次真实请求连续失败时，塔台 2 分钟内补一次 1 token 真实生成探测；探测也失败才判为请求故障并切到备用。请求多的账号仍只按原门槛判断；探测成功或只是模型不存在都不算故障。

- **分组自动切号设置跟随全站**：打开分组设置时显示全站当前值（以前显示写死的旧数字，低价/混池组还会填入 80%/样本 100/连续 50 次），保存时只记下与全站不同的项，其余继续跟随全站；新增「全部跟随全站」。修复「便宜账号恢复后自动换过去」开关读写的字段名不一致（显示永远“已开启”、保存永远“关闭”），补上「一键填入」要用的 10 次选项（以前会被悄悄存成默认值）。

上线建议：只有 1 个账号的分组出故障时无处可切，需要补充同类模型、进价不高于售价的备用账号；请求量少的站点可以调低全站「样本量」和「连续连不上」次数（需在控制台保存）。测试 159/159。
