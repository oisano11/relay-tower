# Security Policy · 安全规范与架构白皮书

中转塔台 (Relay Tower) 作为大模型 (LLM) 聚合中转网关与调度核心，承载了高价值 API 密钥流转、高并发反向代理以及跨渠道自动化容灾。系统在设计上遵循“纵深防御 (Defense-in-Depth)”原则，全面防范权限绕过、凭证泄露、命令注入与跨租户数据污染。

---

## 🛡️ 支持的版本 (Supported Versions)

| 版本 | 是否支持安全更新 | 状态 |
| :--- | :---: | :--- |
| **1.x (main 分支)** | :white_check_mark: | 当前活跃维护主线，包含最新安全加固与容灾修复 |
| < 1.0.0 | :x: | 历史早期测试版本，不建议在生产环境使用 |

---

## 🏰 安全核心架构与八重防御体系

中转塔台内嵌了针对 API 中转网关场景的八重安全加固与隔离机制：

### 1. 🛡️ 网关鉴权与底层 Socket 回环免密体系
- **专属网关密钥 (Gateway Key)**：对外开放的 `/v1/*` 代理接口强制校验 `Authorization: Bearer <Gateway-Key>` 或 `x-api-key`，杜绝未授权外部访问盗刷上游高价值商业 Token。
- **TCP 底层真实 IP 验证**：系统仅对真实来自底层 TCP Socket (`req.socket.remoteAddress`) 的本地回环连接 (`127.0.0.1`, `::1`, `::ffff:127.0.0.1`) 开放免密访问。
- **严禁虚假 Header 越权**：彻底忽略不可信的 `X-Forwarded-For`、`X-Real-IP` 等 HTTP 伪造头，从根本上防止外网攻击者伪造 `X-Forwarded-For: 127.0.0.1` 绕过网关鉴权。

### 2. 🧹 反向代理请求头清洗与上游凭证隔离
- **敏感标头自动剥离**：在转发客户端请求至上游服务商之前，网关自动清洗 `host`、`cookie`、`authorization`、`x-api-key`、`proxy-authorization`、`forwarded` 等客户端头以及 hop-by-hop 连接标头。
- **上游专用鉴权注入**：由网关统一注入当前生效渠道的真实上游 API Key（同时支持 `Authorization: Bearer <Key>` 与 `x-api-key`），客户端永远无法触碰上游真实密钥，彻底阻断跨租户凭据泄露。

### 3. ⚡ 流式生命周期与 TTFT 智能熔断
- **客户端断开即时取消**：当下游客户端主动断开连接（如取消生成、关闭浏览器）时，网关立即向对应上游通道发送 `destroy()` 取消信号，避免无效调用空耗 Token 额度。
- **首字延迟 (TTFT) 熔断**：流式请求由网关内部 SSE 帧探测器严密监控，一旦上游出现假死或超过设定的 `ttftThresholdMs`，系统立刻熔断该通道并规范透传 HTTP 504 Gateway Timeout，绝不向客户端下发伪造的 200 假响应。
- **流式编码安全**：网关强制声明 `Accept-Encoding: identity`；对于未遵守协议的压缩 SSE 异常流执行明确的 502 错误阻断，防止流式事件解析失控。

### 4. ✈️ Telegram Bot 离机中控鉴权与脱敏审计
- **废除免密提权后门**：彻底移除了早期版本中“任意用户发送 `/start` 即可提权为管理员”的逻辑，强制要求管理员通过私聊执行 `/bind <控制台管理密码>` 进行身份验证与密码绑定。
- **群聊与个人权限分离**：通知群（群聊）仅作为报警和报表单向推送通道；即便机器人加入公开群，群内普通成员也绝对无法通过按钮或指令触发切线、查价或调权；敏感操作仅限已白名单绑定的个人 Telegram ID。
- **日志脱敏机制**：系统在记录审计日志与控制台打印时，自动剔除包含密码的 `/bind` 文本，并对展示的所有 API Key 执行前6后4掩码处理（如 `sk-fc3d****9d9e`），杜绝聊天记录与控制台日志泄密。

### 5. 💉 SQL 执行安全与命令注入防御
- **子进程标准输入传输 (stdin Protocol)**：所有面向 PostgreSQL 的 SQL 查询和操作指令，统一通过 `execFileSync` 经由子进程的标准输入 (`stdin`) 传递，严禁在 Shell 命令行中拼接双引号字符串执行，彻底规避 Shell 特殊字符转义失效与命令注入风险。
- **事务一致性保障 (`ON_ERROR_STOP=1`)**：数据库操作严格开启 `ON_ERROR_STOP=1` 并在必要路径启用 `BEGIN ... COMMIT` 事务包裹。SQL 执行失败时必须立即上抛异常并回滚本地状态，彻底消除“底层操作已失败，界面却误报成功”的状态脱节隐患。

### 6. 🔒 多分组调度安全与防误杀架构
- **全面支持一个渠道挂载多个业务组**：废除了对跨组多绑账号的粗暴 409 拦截，完美支持一个高性价比渠道在多个销售组之间共享复用。
- **单主独占优雅降级**：在适用业务组开启“单主独占（锁定 Prompt Cache）”模式时，系统仅对只属于当前组的独立渠道关停调度 (`schedulable = false`)；而对挂载了其他业务组的共享渠道，**自动保留全局调度开关，仅调低组内调度优先级 (`priority = 10`)**。
- **动态当前组毛利核算**：通道的成本与利润严格以当前触发业务组的销售倍率进行实时核算，杜绝以偏概全导致的亏损倒贴。

### 7. 🛡️ 上游模型审批精准隔离
- **渠道定向挂载**：中控台扫描发现上游上线新模型时，管理员审批仅将该模型映射 (`modelMapping`) 精确同步至**实际提供该模型的来源渠道**并写入数据库，彻底杜绝早期版本中“审批一个新模型导致全场所有活跃渠道被无脑污染”的严重逻辑缺陷。

### 8. 💾 磁盘文件原子级持久化
- **原子替换防截断**：对于 `channels.json`、`auto_switch_config.json` 等关键数据，写盘操作均采用写入随机 `.tmp` 临时文件后执行操作系统的 `renameSync` 原子重命名，彻底规避高并发并发写盘、服务器异常断电或强制杀进程导致的文件内容截断清空风险。

---

### 9. 🛡️ 运行时鲁棒性与畸形输入防御
- **Cookie 解析容错**：对传入的 HTTP Cookie 键值提供容错保护，严密包裹 `decodeURIComponent`，防止外部畸形 Cookie（如 `any=%`）抛出未捕获异常引发 Node.js 主服务崩溃。
- **统一错误兜底拦截**：未捕获的路由与网关异常由顶层中间件规范拦截，输出标准 JSON 错误响应，保障守护进程持久高可用。

---

## 🔒 生产环境安全部署合规清单

将中转塔台部署于公网 VPS 或生产集群时，请务必完成以下合规加固：

1. **设置强密码**：
   - 启动前在 `.env` 或环境变量中设置高强度 `ADMIN_PASSWORD`、`POSTGRES_PASSWORD`、`REDIS_PASSWORD`、`JWT_SECRET` 和 `TOTP_ENCRYPTION_KEY`。
2. **端口防火墙隔离**：
   - **PostgreSQL (5432)** 与 **Redis (6379)** 仅限容器内网访问，绝对不可映射暴露至公网宿主机。
   - 中控台与网关端口 (`3300`) 建议通过 Nginx / Caddy 反向代理，并开启 HTTPS / TLS 加密传输。
3. **启用专属 Gateway API Key**：
   - 在中控台【安全设置】中配置并启用 Gateway API Key，对外接口统一强制校验密钥。
4. **敏感目录权限保护**：
   - 确保 `upstream-monitor/data/` 目录与 `.env` 配置文件受到严格的文件系统权限保护（建议权限 `600` 或 `700`），禁止 Web 服务器将其作为静态资源直接对外提供下载。
5. **Telegram 机器人安全**：
   - 使用私聊与机器人互动，并在私聊中发送 `/bind <管理密码>` 绑定个人 ID。切勿在公开大群中发送绑定命令或泄露管理员密码。

---

## 🚨 报告安全漏洞 (Reporting a Vulnerability)

如果您在本项目中发现了新的安全缺陷或漏洞（包括权限绕过、SSRF、凭据泄露等）：

1. **请切勿直接提交公开 Issue 或公开讨论**；
2. 请直接通过项目维护者的私密联系渠道或 Telegram 进行责任披露；
3. 请在报告中提供：
   - 漏洞组件与影响范围；
   - 最小化复现步骤或 PoC；
   - 修复思路或建议补丁（如有）；
4. 我们会在收到报告后的 **24 小时内** 完成验证并启动修复流程。
